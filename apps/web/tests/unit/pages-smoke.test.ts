import { describe, it } from 'vitest';

describe('Phase 4 page-render smoke (all 6 pages)', () => {
  it.todo(
    '/dashboard/chat renders thread list + active thread with mocked SWR data — Wave 4 Plan 04-04'
  );
  it.todo(
    '/dashboard/calls renders table + filter bar + opens modal on row click — Wave 4 Plan 04-04'
  );
  it.todo(
    '/dashboard/orders renders table with city names + filter changes update URL — Wave 5 Plan 04-05'
  );
  it.todo(
    '/dashboard/orders/[id] renders timeline + channel breadcrumb (voice/telegram) — Wave 5 Plan 04-05'
  );
  it.todo('/dashboard/default renders compact KPI tiles + last-5 lists — Wave 5 Plan 04-05');
  it.todo(
    '/dashboard/analytics renders charts + window selector updates ?window= — Wave 5 Plan 04-05'
  );
});
